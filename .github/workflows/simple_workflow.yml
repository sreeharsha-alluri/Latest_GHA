name: New Workflow
on:
  push:
    branches:
      - main

jobs:
  build-and-notify:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build
        shell: bash
        run: echo "Building"

      - name: Test
        shell: bash
        run: echo "Testing"

      # - name: Error
      #   shell: bash
      #   run: exit 1

      - name: Print environment variables
        run: |
          echo "ERROR_MENTION_ID: ${{ env.ERROR_MENTION_ID }}"
          echo "ERROR_MENTION_NAME: ${{ env.ERROR_MENTION_NAME }}"
          echo "SUCCESS_MENTION_ID: ${{ env.SUCCESS_MENTION_ID }}"
          echo "SUCCESS_MENTION_NAME: ${{ env.SUCCESS_MENTION_NAME }}"

      - name: Send notification to Microsoft Teams
        if: always()
        uses: ./.github/actions/build_and_notify
        with:
          action-name: ${{ github.workflow }}
          error-mention-id: ${{ env.ERROR_MENTION_ID }}
          error-mention-name: ${{ env.ERROR_MENTION_NAME }}
          success-mention-id: ${{ env.SUCCESS_MENTION_ID }}
          success-mention-name: ${{ env.SUCCESS_MENTION_NAME }}
